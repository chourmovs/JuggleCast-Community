---
import { withBase } from '../lib/paths';
import BaseLayout from '../layouts/BaseLayout.astro';
---
<BaseLayout title="Page not found — JuggleCast" description="The requested JuggleCast website page could not be found." noindex={true}><section class="page container"><header class="page-head"><p class="eyebrow">404</p><h1>Page not found</h1><p>The address may have changed. Return to the <a href={withBase('')}>JuggleCast home page</a> or browse <a href={withBase('features')}>documented features</a>.</p></header></section></BaseLayout>
